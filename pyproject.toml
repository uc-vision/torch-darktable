[build-system]
requires = ["hatchling", "torch"]
build-backend = "hatchling.build"

[project]
name = "cuda_demosaic"
version = "0.1.0"
description = "CUDA Demosaic algorithms extracted from darktable for PyTorch"
authors = [{name = "Oliver Batchelor"}]
requires-python = ">=3.10"
dependencies = [
    "torch>=2.7.0",
    "numpy>=2.2.0",
]
